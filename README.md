# sillybird
C++ webapp server using the [Crow](https://crowcpp.org/master/) library.

## Requirements
* `g++` compiler.
* `Makefile`.

## Build
Running `make` should suffice. The Crow library and their dependencies headers are included in this project under the `ìnclude/` project.

Note: if it gets out of hand (compilation issues, etc), use `vcpkg`. Crow creator has a project template: https://github.com/gittiver/crow_template
